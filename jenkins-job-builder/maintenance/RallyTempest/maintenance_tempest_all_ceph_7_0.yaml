- job:
    name: maintenance_tempest_all_ceph_7_0
    builders:
      - trigger-builds:
        - project: 'create_param_environment_maintenance_7_0'
          same-node: true
          block: true
          predefined-parameters: |
            ENV_NAME=maintenance_tempest_all_ceph_7_0
            TEST_TYPE=TEMPEST
            FILE=test_tempest_ceph_services.py
            GROUP=tempest_ceph_services
            ERASE_PREV_ENV=$delete_old_env
      - trigger-builds:
        - project: 'maintenance_tempest_runner'
          same-node: true
          block: true
          block-thresholds:
            build-step-failure-threshold: 'never'
          predefined-parameters: |
            ENV_NAME=maintenance_tempest_all_ceph_7_0
            SNAPSHOT_NAME=tempest_test_ceph
            REPORT_PREFIX=/srv/jenkins/rally-tempest
    project-type: freestyle
    defaults: global
    node: maintenance_173_13
    description: 'VLAN+CEPH+SAHARA+MURANO_CEILOMETER_RADOS_MONGO TEMPEST tests (7.0 maintenance)'
    disabled: false
    display-name: 'VLAN+CEPH+SAHARA+MURANO_CEILOMETER_RADOS_MONGO TEMPEST tests (7.0 maintenance)'
    concurrent: false
    retry-count: 3
    logrotate:
      daysToKeep: 5
      numToKeep: 20
      artifactDaysToKeep: -1
      artifactNumToKeep: -1

