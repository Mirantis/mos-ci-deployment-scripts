- job:
    name: 9.0_superjob_for_VLAN_CEPH_RADOS_TEMPEST_scenarion_only
    builders:
      - trigger-builds:
        - project: '9.0_rally_tempest_tests_scenario_for_VLAN_CINDER'
          same-node: true
          block: true
          block-thresholds:
            build-step-failure-threshold: 'never'
          predefined-parameters: |
            SNAPSHOT_NAME=ha_deploy_VLAN_CEPH_GLANCE_CEPH
            REPORT_PREFIX=/srv/jenkins/rally-tempest/scenario
            MOS_VERSION=all
            CEPH_RADOS=TRUE
            CEPH_SKIP_TESTS=TRUE
#
#      - trigger-builds:
#        - project: 'tempest_test_rail_reporter'
#          same-node: true
#          block: true
#          predefined-parameters: |
#            SNAPSHOT_NAME=ha_deploy_VLAN_CEPH_GLANCE_CEPH
#            TEST_GROUP=TEMPEST, Ceph(scenario)
#            REPORT_PREFIX=/srv/jenkins/rally-tempest/scenario
#            REPORT_FILE=verification.xml
#            SUITE=[9.0][MOSQA] Tempest 9.0
#            MILESTONE=9.0
#            MOS_VERSION=all

    project-type: freestyle
    defaults: global
    node: Slave-srv139
    description: 'VLAN TEMPEST tests'
    disabled: false
    display-name: 'superjob-9.0-RALLY-TEMPEST VLAN_CEPH_RADOS_1contr_1compute(scenario only)'
    concurrent: false
    retry-count: 3
    logrotate:
      daysToKeep: 5
      numToKeep: 20
      artifactDaysToKeep: -1
      artifactNumToKeep: -1
